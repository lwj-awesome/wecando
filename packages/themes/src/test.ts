import { colors } from ".";

const RedTextColor = colors.palette.red[500];
const BlueTextColor = colors.palette.blue[500];
const YellowTextColor = colors.palette.yellow[500];

export { RedTextColor, BlueTextColor, YellowTextColor };
